# Руководство по автоматическим миграциям

Это руководство описывает новую систему автоматических миграций базы данных для Telegram бота.

## Обзор системы

Наша система миграций обеспечивает:

- ✅ **Автоматические миграции** при запуске Docker контейнера
- ✅ **Автоматические откаты** при ошибках миграций
- ✅ **Интеграцию с CI/CD** pipeline
- ✅ **Минимальные простои** приложения
- ✅ **Надежность** и безопасность операций
- ✅ **Мониторинг** и алерты

## Архитектура

### Компоненты

1. **Migration Scripts** (`scripts/`)
   - `migration-health.sh` - Проверка состояния миграций
   - `deploy-migration.sh` - Безопасное применение миграций
   - `backup-before-migration.sh` - Создание бэкапов перед миграцией
   - `migration-monitor.sh` - Мониторинг состояния
   - `restore-from-backup.sh` - Восстановление из бэкапа

2. **Smart Startup Script** (`start.sh`)
   - Автоматическая проверка миграций при запуске
   - Применение миграций при необходимости
   - Автоматический откат при ошибках

3. **Docker Integration**
   - Health checks для базы данных
   - Правильная очередность запуска сервисов
   - Volume монтирования для бэкапов

4. **CI/CD Integration**
   - Проверка миграций перед развертыванием
   - Автоматическое применение в production
   - Валидация после развертывания

## Использование

### Локальная разработка

```bash
# Проверить статус миграций
npm run migration:check

# Применить миграции (development mode)
npm run prisma:migrate

# Создать новую миграцию
npx prisma migrate dev --name "migration_name"

# Проверить здоровье миграций
npm run migration:monitor

# Создать бэкап перед миграцией
npm run migration:backup
```

### Production развертывание

```bash
# Ручное развертывание с миграциями
./deploy.sh

# Применить миграции вручную
npm run migration:deploy

# Проверить состояние после развертывания
npm run migration:check

# Запустить мониторинг
npm run migration:monitor
```

### Восстановление и откат

```bash
# Показать доступные бэкапы
npm run migration:restore list

# Восстановить из последнего бэкапа
npm run migration:restore latest

# Восстановить из конкретного бэкапа
npm run migration:restore ./backups/pre_migration_20231201_120000.sql.gz
```

### Мониторинг

```bash
# Запустить разовую проверку
npm run migration:monitor check

# Запустить непрерывный мониторинг
npm run migration:monitor continuous

# Сгенерировать отчет
npm run migration:monitor report

# Очистить старые логи
npm run migration:monitor cleanup
```

## CI/CD Process

### GitHub Actions Workflow

1. **Pre-deploy Checks**
   - Валидация Prisma schema
   - Проверка наличия файлов миграций
   - Build проекта

2. **Migration Validation**
   - Запуск тестовой базы данных
   - Применение миграций на тестовую БД
   - Валидация схемы

3. **Deploy**
   - Применение миграций в production
   - Запуск приложения
   - Проверка состояния

### Развертывание

Развертывание теперь включает:

1. 🔄 Подготовка к миграциям
2. 🚀 Запуск базы данных
3. ⏳ Ожидание готовности БД
4. 🔄 Применение миграций (с откатом при ошибках)
5. 🚀 Запуск приложения
6. ✅ Проверка состояния

## Безопасность

### Автоматические бэкапы

- Создаются перед каждой миграцией
- Хранятся в `./backups/`
- Автоматически сжимаются (gzip)
- Удаляются старые бэкапы (хранятся последние 5)

### Автоматические откаты

- При неудачной миграции происходит откат
- Используется последний бэкап
- Приложение не запускается с некорректной схемой

### Валидация

- Проверка целостности бэкапов
- Валидация схемы после миграции
- Health checks для базы данных

## Troubleshooting

### Частые проблемы

#### 1. Миграции не применяются

```bash
# Проверить статус
npm run migration:check

# Показать детали
npx prisma migrate status

# Применить вручную
npm run migration:deploy
```

#### 2. Ошибка подключения к БД

```bash
# Проверить переменные окружения
echo $DATABASE_URL

# Проверить доступность БД
./scripts/wait-for-it.sh localhost:5432 --timeout=60 --strict -- echo "Database is ready"
```

#### 3. Приложение не запускается после миграции

```bash
# Проверить логи
docker compose -f docker-compose.prod.yml logs bot

# Восстановить из бэкапа
npm run migration:restore latest

# Проверить миграции
npm run migration:check
```

#### 4. CI/CD не проходит

```bash
# Локально проверить миграции
npm run migration:check

# Валидировать схему
npx prisma validate

# Проверить сборку
npm run build
```

### Логирование

- Логи миграций: `./logs/migration-monitor.log`
- Логи ошибок: `./logs/migration-errors.log`
- Docker логи: `docker compose logs bot`

### Алерты

Настройте вебхук для алертов:

```bash
export MIGRATION_ALERT_WEBHOOK="https://your-webhook-url"
npm run migration:monitor
```

## Best Practices

### Разработка

1. Всегда создавайте миграции через `npx prisma migrate dev`
2. Тестируйте миграции локально перед коммитом
3. Проверяйте статус миграций перед развертыванием
4. Создавайте бэкапы перед критическими изменениями

### Production

1. Используйте CI/CD для автоматического применения миграций
2. Мониторьте состояние миграций
3. Регулярно проверяйте бэкапы
4. Имейте план отката для критических ситуаций

### Безопасность

1. Храните DATABASE_URL в безопасном месте
2. Ограничьте доступ к бэкапам
3. Регулярно тестируйте восстановление из бэкапов
4. Используйте разные БД для разработки и production

## Конфигурация

### Environment Variables

```bash
DATABASE_URL=postgresql://user:password@host:port/database
MIGRATION_ALERT_WEBHOOK=https://your-webhook-url
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=notification_bot
```

### Docker Configuration

- Health checks включены
- Правильные зависимости между сервисами
- Volume монтирования для бэкапов
- Timeout настройки для запуска

## Поддержка

При возникновении проблем:

1. Проверьте логи в `./logs/`
2. Запустите `npm run migration:monitor check`
3. Посмотрите Docker логи: `docker compose logs bot`
4. Создайте issue в репозитории с подробным описанием